[plugin]
id = "open-url"
name = "Open URL"
description = "Detect and open URLs typed in the search bar"
version = "0.1.0"
wasm = "open_url_plugin.wasm"
icon = "heroicons:globe-alt"

[permissions]
website-metadata = true

[permissions.opener]
schemes = ["https", "http"]
